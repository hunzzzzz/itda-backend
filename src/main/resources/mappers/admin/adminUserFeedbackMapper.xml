<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moira.itda.domain.admin_user_feedback.mapper.AdminUserFeedbackMapper">
    <!-- 피드백 목록 조회 -->
    <select id="selectFeedbackList" resultType="com.moira.itda.domain.admin_user_feedback.dto.response.AdminUserFeedbackResponse">
        SELECT
               UF.id       AS feedback_id
             , UF.user_id
             , U.nickname  AS user_nickname
             , UF.type
             , UF.content
             , UF.file_id
             , I.file_url
             , UF.created_at
             , UF.answer_content
             , UF.answered_at
        FROM USER_FEEDBACK UF
         INNER JOIN USER U        ON U.id       = UF.user_id
         LEFT JOIN  IMAGE_FILE I  ON I.file_id  = UF.file_id
        ORDER BY UF.created_at DESC
    </select>

    <!-- 피드백 답변 > Feedback 수정 -->
    <update id="updateFeedbackAnswer">
        UPDATE USER_FEEDBACK
        SET
               answer_content = #{answerContent}
             , answered_at    = CURRENT_TIMESTAMP
             , updated_at     = CURRENT_TIMESTAMP
        WHERE id = #{feedbackId}
    </update>
</mapper>