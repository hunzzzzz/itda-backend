<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moira.itda.domain.user.mysuggest.mapper.MySuggestMapper">
    <!-- 마이페이지 > 내 거래 목록 > 거래 제안 목록 탭 > 거래 제안 목록 조회 > 거래 목록 조회 > totalElements 계산 -->
    <select id="selectTradeSuggestListCnt">
        SELECT COUNT(*) FROM TRADE_SUGGEST WHERE user_id = #{userId}
    </select>

    <!-- 마이페이지 > 내 거래 목록 > 거래 제안 목록 탭 > 거래 제안 목록 조회 > 거래 목록 조회 -->
    <select id="selectTradeSuggestList" resultType="com.moira.itda.domain.user.mysuggest.dto.response.MySuggestResponse">
        SELECT
               T.id                AS trade_id
             , T.status            AS trade_status
             , T.type
             , T.user_id
             , U.nickname          AS user_nickname
             , T.file_id
             , F.file_url
             , T.created_at        AS trade_created_at
             , TS.gacha_id
             , TS.id               AS suggest_id
             , TS.status           AS suggest_status
             , TS.content          AS suggest_content
             , TS.created_at       AS suggest_created_at
             , TS.purchase_item_id AS purchase_gacha_item_id
             , (SELECT name FROM GACHA_ITEM WHERE id = TS.purchase_item_id)           AS purchase_gacha_item_name
             , TS.purchase_count
             , TS.purchase_discount_yn
             , TS.purchase_discount_price
             , TS.exchange_seller_item_id
             , (SELECT name FROM GACHA_ITEM WHERE id = TS.exchange_seller_item_id)    AS exchange_seller_item_name
             , TS.exchange_suggested_item_id
             , (SELECT name FROM GACHA_ITEM WHERE id = TS.exchange_suggested_item_id) AS exchange_suggested_item_name
             , TS.exchange_change_yn
        FROM TRADE_SUGGEST TS
        INNER JOIN TRADE T       ON T.id      = TS.trade_id
        INNER JOIN USER U        ON U.id      = T.user_id
        LEFT JOIN IMAGE_FILE F   ON F.file_id = T.file_id
    </select>

    <!-- 마이페이지 > 내 거래 목록 > 거래 제안 목록 탭 > 거래 제안 취소 > 구매 status 조회 -->
    <select id="selectPurchaseSuggestStatus" resultType="java.lang.String">
        SELECT status FROM TRADE_PURCHASE_SUGGEST
        WHERE id = #{suggestId} AND user_id = #{userId}
    </select>

    <!-- 마이페이지 > 내 거래 목록 > 거래 제안 목록 탭 > 거래 제안 취소 > 교환 status 조회 -->
    <select id="selectExchangeSuggestStatus" resultType="java.lang.String">
        SELECT status FROM TRADE_EXCHANGE_SUGGEST
        WHERE id = #{suggestId} AND user_id = #{userId}
    </select>

    <!-- 마이페이지 > 내 거래 목록 > 거래 제안 목록 탭 > 거래 제안 취소 > 구매 status 변경 -->
    <update id="updatePurchaseSuggestStatusCanceled">
        UPDATE TRADE_PURCHASE_SUGGEST SET status = 'CANCELED'
        WHERE id = #{suggestId} AND user_id = #{userId}
    </update>

    <!-- 마이페이지 > 내 거래 목록 > 거래 제안 목록 탭 > 거래 제안 취소 > 교환 status 변경  -->
    <update id="updateExchangeSuggestStatusCanceled">
        UPDATE TRADE_EXCHANGE_SUGGEST SET status = 'CANCELED'
        WHERE id = #{suggestId} AND user_id = #{userId}
    </update>
</mapper>