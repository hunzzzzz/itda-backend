<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moira.itda.domain.user.support.mapper.UserSupportMapper">
    <!-- 문의 등록 > UserSupport 저장 -->
    <insert id="insertUserSupport">
        INSERT INTO USER_SUPPORT (
               user_id
             , type
             , content
             , file_id
             , created_at
             , updated_at
             , answer_content
             , answered_at
        )
        VALUES (
               #{userId}
             , #{type}
             , #{content}
             , #{fileId}
             , #{createdAt}
             , #{updatedAt}
             , #{answerContent}
             , #{answeredAt}
        )
    </insert>

    <!-- 문의 목록 조회 > totalElements 계산 -->
    <select id="selectUserSupportListCnt">
        SELECT COUNT(id) FROM USER_SUPPORT WHERE user_id = #{userId}
    </select>

    <!-- 문의 목록 조회 -->
    <select id="selectUserSupportList" resultType="com.moira.itda.domain.user.support.dto.response.SupportResponse">
        SELECT
               US.id AS support_id
             , US.user_id
             , US.type
             , US.content
             , US.file_id
             , NULL AS file_urls
             , US.created_at
             , US.answer_content
             , US.answered_at
        FROM USER_SUPPORT US
        WHERE US.user_id = #{userId}
        ORDER BY US.created_at DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>
</mapper>