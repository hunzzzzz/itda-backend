<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moira.itda.domain.user_temp.mypage.mapper.MyPageMapper">
    <!-- 마이페이지 > 내 프로필 조회 -->
    <select id="selectMyPageResponse" resultType="com.moira.itda.domain.user_temp.mypage.dto.response.MyPageResponse">
        SELECT
               U.id AS userId
             , U.role
             , U.status
             , U.email
             , U.name
             , U.nickname
             , U.file_id
             , F.file_url AS imageFileUrl
             , U.last_login_at
             , U.created_at
        FROM USER U
        LEFT JOIN IMAGE_FILE F
         ON F.file_id = U.file_id
        WHERE U.id = #{userId}
    </select>

    <!-- 마이페이지 > 프로피 사진 변경 > 파일 ID 수정 -->
    <select id="selectCurrentFileUrl" resultType="java.lang.String">
        SELECT F.file_url
        FROM IMAGE_FILE F
        LEFT JOIN USER U
         ON U.file_id = F.file_id
        WHERE U.id = #{userId}
    </select>

    <!-- 마이페이지 > 프로피 사진 변경 > 파일 ID 수정 -->
    <update id="updateFileId">
        UPDATE USER SET file_id = #{newFileId} WHERE id = #{userId}
    </update>

    <!-- 마이페이지 > 닉네임 변경 > 닉네임 존재 여부 확인 -->
    <select id="selectNicknameChk">
        SELECT COUNT(id) FROM USER WHERE nickname = #{nickname}
    </select>

    <!-- 마이페이지 > 닉네임 변경 -->
    <update id="updateNickname">
        UPDATE USER SET nickname = #{newNickname} WHERE id = #{userId}
    </update>

    <!-- 마이페이지 > 비밀번호 변경 > 현재 비밀번호 조회 -->
    <select id="selectCurrentPassword" resultType="java.lang.String">
        SELECT password FROM USER WHERE id = #{userId}
    </select>

    <!-- 마이페이지 > 비밀번호 변경 -->
    <update id="updatePassword">
        UPDATE USER SET password = #{newPassword} WHERE id = #{userId}
    </update>
</mapper>