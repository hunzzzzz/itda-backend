<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moira.itda.domain.trade.detail.mapper.TradeDetailMapper">
    <!-- 거래상세정보 조회 -->
    <select id="selectTradeDetail" resultType="com.moira.itda.domain.trade.detail.dto.response.TradeDetailResponse">
        SELECT
               T.id          AS trade_id
             , T.gacha_id
             , T.type
             , T.status
             , T.title
             , T.content
             , T.file_id
             , NULL          AS file_url_list
             , T.hope_method
             , T.hope_location
             , T.hope_address
             , T.hope_location_latitude
             , T.hope_location_longitude
             , T.created_at
        FROM TRADE T
        WHERE id = #{tradeId}
    </select>
</mapper>