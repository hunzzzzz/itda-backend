<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatiG.org//DTD Mapper 3.0//EN"
        "https://mybatiG.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moira.itda.domain.gacha.detail.mapper.GachaDetailMapper">
    <!-- 가챠정보 > 가챠목록 > 상세정보 > 가챠 조회 -->
    <select id="getGacha" resultType="com.moira.itda.domain.gacha.detail.dto.response.GachaResponse">
        SELECT
               G.id AS gachaId
             , G.status
             , G.title
             , G.manufacturer
             , G.file_id
             , F.file_url
             , G.price
             , G.view_count
             , G.created_at
        FROM GACHA G
        LEFT JOIN IMAGE_FILE F
         ON F.file_id = G.file_id
        WHERE G.id = #{gachaId}
    </select>

    <!-- 가챠정보 > 가챠목록 > 상세정보 > 하위 아이템 목록 조회 -->
    <select id="getGachaItemList" resultType="com.moira.itda.domain.gacha.detail.dto.response.GachaItemResponse">
        SELECT
               id AS gachaItemId
             , name
        FROM GACHA_ITEM
        WHERE gacha_id = #{gachaId}
        ORDER BY created_at ASC
    </select>

    <!-- 가챠정보 > 가챠목록 > 상세정보 > 조회수 증가 -->
    <update id="updateViewCount">
        UPDATE GACHA SET view_count = view_count + 1 WHERE id = #{gachaId}
    </update>
</mapper>