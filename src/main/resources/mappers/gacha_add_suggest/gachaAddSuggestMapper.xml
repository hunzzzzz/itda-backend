<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moira.itda.domain.gacha_add_suggest.mapper.GachaAddSuggestMapper">
    <!-- 정보등록요청 > GachaAddSuggest 저장 -->
    <insert id="insertGachaAddSuggest">
        INSERT INTO GACHA_ADD_SUGGEST (
               id
             , user_id
             , status
             , content
             , file_id
             , admin_comment
             , receive_email_yn
             , requested_at
             , processed_at
        )
        VALUES (
               #{id}
             , #{userId}
             , #{status}
             , #{content}
             , #{fileId}
             , #{adminComment}
             , #{receiveEmailYn}
             , #{requestedAt}
             , #{processedAt}
        )
    </insert>
</mapper>